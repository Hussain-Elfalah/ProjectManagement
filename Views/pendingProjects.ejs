<!-- Include the sidebar -->
<%- include('partials/sidebar') %>

<div id="pending-container">
    <h1 id="pending-heading">Pending Projects</h1>
    <!-- Table of Pending Projects -->
    <table>
        <thead>
            <tr>
                <th>Project Name</th>
                <th>Submitter</th>
                <th>Created at</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            <!-- Loop through pending projects data and populate the table -->
            <% project.forEach(function(project) { %>
            <tr>
                <td><%= project.project_name %></td>
                <td><%= project.submitter_name %></td>
                <td><%= project.submission_timestamp %></td>
                <td>
                    <form method="post" action="/activeprojects/add">
                        <% console.log(project); %>
                        <input type="hidden" name="pending_project_id" value="<%= project.pending_project_id %>">
                        <input type="hidden" name="project_id" value="<%= project.project_id %>">
                        <input type="hidden" name="charter_id" value="<%= project.charter_id %>">
                        <input type="hidden" name="submitter_id" value="<%= project.submitter_id %>">
                        <button type="submit">Approve</button>
                    </form>
                    
                    <form method="post" action="#">
                        <button type="submit">Reject</button>
                    </form>
                </td>
            </tr>
            <% }); %>
        </tbody>
    </table>
</div>

<!-- Include the footer -->
<%- include('partials/footer') %>
